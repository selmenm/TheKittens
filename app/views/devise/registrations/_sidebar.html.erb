<!-- Account Sidebar-->
<div class="col-lg-4 pb-5">
  <div class="author-card pb-3">
    <div class="author-card-cover" style="background-image: url(img/widgets/author/cover.jpg);">
      <span class="btn btn-light btn-sm" data-toggle="tooltip" title="You currently have bought <%= Cart.where(user_id: @user.id).where.not(order_id: nil).length %> items">
        <i class="fas fa-shopping-cart"></i>&nbsp;<%= Cart.where(user_id: user.id).where.not(order_id: nil).length %> item(s) bought
      </span>
    </div>

    <div class="author-card-profile">
      <div class="author-card-avatar">
        <%if user.avatar.attached?%>
          <%= image_tag user.avatar, alt: @user.fullname %>
        <%else%>
          <%= image_tag("default_avatar.svg") %> 
        <%end%>
      </div>
      <div class="author-card-details">
        <h5 class="author-card-name text-lg"><%=user.fullname%></h5><span class="author-card-position">Joined <%=@user.created_at.strftime("%B %d, %Y")%></span>      
      </div>
    </div>
  </div>

  <div class="wizard">
    <nav class="list-group list-group-flush">
      <a class="list-group-item active" href="<%= my_profile_path %>"><i class="far fa-user"></i>Profile Settings</a>
      <a class="list-group-item" href="#"><i class="fas fa-lock"></i>Security</a>
      <a class="list-group-item" href="account-wishlist.html">
        <div class="d-flex justify-content-between align-items-center">
          <div><i class="fas fa-shopping-cart"></i>
            <div class="d-inline-block font-weight-medium text-uppercase">My Cart</div>
          </div>
          <span class="badge badge-secondary"><%= Cart.where(user_id: user.id).length%></span>
        </div>
      </a>
      <a class="list-group-item" href="account-wishlist.html">
        <div class="d-flex justify-content-between align-items-center">
          <div><i class="far fa-heart"></i>
            <div class="d-inline-block font-weight-medium text-uppercase">My Wishlist</div>
          </div>
          <span class="badge badge-secondary">100</span>
        </div>
      </a>
    </nav>
  </div>
</div>

<%= javascript_include_tag "change" %>